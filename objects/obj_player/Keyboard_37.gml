
/*
if (isMoving == true) {
    // If the player is walking, play the walking sound
    if (!audio_is_playing(snd_walktest)) {
         audio_play_sound(snd_walktest, 1 ,false)
    }
} else {
    // If the player is not walking, pause the walking sound if it's playing
    if (isMoving == false) {
        audio_pause_sound(snd_walktest);
    }
}
*/
vel_x = -move_speed

	